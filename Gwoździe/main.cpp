#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int gwozdzi, uderzen,ile;

int main() {
	cin >> gwozdzi >> uderzen;
	int wysokosc[1000];
	for(int i = 0; i < gwozdzi; i++) {
		cin >> ile;
		wysokosc[ile]++;
	}
	for(int i = 0; i < 1000; i++) {
		if(wysokosc[i] > 0) {
			if(wysokosc[i] + uderzen <= 

		}
	}
}
